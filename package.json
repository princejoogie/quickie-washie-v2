{
  "name": "quickie-washie-v2",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "cache:clean": "turbo run cache:clean --parallel",
    "clean": "turbo run clean --parallel",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "generate": "turbo run generate --parallel",
    "lint": "turbo run lint",
    "postinstall": "yarn workspace @qw/db generate"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.19"
}
